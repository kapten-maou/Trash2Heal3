import 'package:flutter/material.dart';

class MembershipPointsScreen extends StatelessWidget {
  const MembershipPointsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Membership & Points page coming soon',
        style: TextStyle(fontSize: 16, fontWeight: FontWeight.w600),
      ),
    );
  }
}
